
#include <iostream>

using namespace std;
int fibo(int y);
int main()
{
   int i;
   cout<<"sayı gir";
   cin>>i;
   cout<<"fibonacci "<<fibo(i);
    
}
int fibo(int y){
    if(y<0)
      return -1;
    if (y==0)
      return 0;
    if (y==1)
      return 1;
     return fibo(y-1)+fibo(y-2); 
}
